> 以下所有内容参考自https://www.bilibili.com/video/BV11g411V7Kf
## 接口

> 系统、组件之间，数据传递的通道

### 1.1 接口的类型

- 按协议划分：http、tcp、ip
- 按语言划分：java、c++
- 按范围划分：系统之间的接口、程序（方法、模块）之间的接口

```python
#接口
def eat(result):
    print(f'小鸟吃{result}')
#使用接口
def bind():
    eat('apple')
bind()
```