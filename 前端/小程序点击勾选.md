```html
<view wx:for="{{orderList}}" class="order">
	<image src="{{item.id == checkOrder ? './imges/check.png' : './imges/by.png'}}" mode="" class="check" bind:tap="check" data-index="{{index}}"/>
</view>
```

```js
check: function (e) {
    this.setData({
      checkOrder: e.currentTarget.dataset.index
    })
  },
```

